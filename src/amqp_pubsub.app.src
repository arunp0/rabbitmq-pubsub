{application, amqp_pubsub, [
    {description, "An Amqp Client"},
    {vsn, git},
    {registered, []},
    {applications, [kernel,
                    stdlib,
                    xmerl,
                    syntax_tools,
                    recon,
                    compiler,
                    rabbit_common,
                    amqp_client
    ]},
    {env, []}
]}.
